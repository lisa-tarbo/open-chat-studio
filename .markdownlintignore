# Markdown that is consumed by tooling rather than published to the docs site.
# Markdown that is consumed by AI-Agents does need to follow appropriate rules
# Some folders are set to be ignored as we rollout fixes

# Workflow templates - a top-level heading would change their meaning, so MD041 must not apply to them.
.github/ISSUE_TEMPLATE/
.github/pull_request_template.md

# Superpowers & designs (for now)
docs/superpowers
docs/design

# CLAUDE.md is just an @-include pointing at AGENTS.md, so it fails MD041 (no H1)
CLAUDE.md

# Ignore Claude skills files (for now)
.claude/skills/
.claude/skills/extract-adrs/templates

# Any markdown in the apps or components directory  (for now)
/apps/
/components/

# Build output and virtualenv
site/
.venv/
